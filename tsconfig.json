{
  "extends": "@tsconfig/ember/tsconfig.json",
  "compilerOptions": {
    // The combination of `baseUrl` with `paths` allows Ember's classic package
    // layout, which is not resolvable with the Node resolution algorithm, to
    // work with TypeScript.
    "baseUrl": ".",
    "paths": {
      "dummy/tests/*": ["tests/*"],
      "dummy/*": ["tests/dummy/app/*", "app/*"],
      "velvet-thunder": ["addon"],
      "velvet-thunder/*": ["addon/*"],
      "velvet-thunder/test-support": ["addon-test-support"],
      "velvet-thunder/test-support/*": ["addon-test-support/*"],
      "*": ["types/*"]
    },
    // Temp because of https://github.com/josemarluedke/docfy/issues/164.
    "skipLibCheck": true
  },
  "glint": {
    "environment": "ember-loose"
  },
  "include": [
    "app/**/*",
    "addon/**/*",
    "tests/**/*",
    "types/**/*",
    "test-support/**/*",
    "addon-test-support/**/*"
  ]
}
